---
phase: 24-project-dashboard-crud
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/page.tsx
  - src/app/project/[id]/page.tsx
  - src/app/project/[id]/client.tsx
  - src/hooks/useToast.ts
  - src/components/Toast.tsx
  - src/lib/musicxmlValidation.ts
autonomous: true

must_haves:
  truths:
    - "Dashboard page renders at / (root route) as a server component with force-dynamic"
    - "Editor page renders at /project/[id] with the existing App component loaded client-side"
    - "Editor page has a Dashboard back button and Sign out button"
    - "Toast system supports an action button with label and onClick callback"
    - "isLikelyMusicXML accepts MEI root elements (mei, music) in addition to MusicXML elements"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Dashboard server component shell at root route"
      contains: "force-dynamic"
    - path: "src/app/project/[id]/page.tsx"
      provides: "Editor page server component shell"
      contains: "force-dynamic"
    - path: "src/app/project/[id]/client.tsx"
      provides: "Client wrapper for editor with Dashboard button and Sign out"
      contains: "signOut"
    - path: "src/hooks/useToast.ts"
      provides: "Toast hook with action support"
      contains: "action"
    - path: "src/components/Toast.tsx"
      provides: "Toast component rendering action button"
      contains: "action"
  key_links:
    - from: "src/app/project/[id]/client.tsx"
      to: "src/App.tsx"
      via: "dynamic import of App component"
      pattern: "dynamic.*import.*App"
    - from: "src/app/project/[id]/client.tsx"
      to: "src/lib/firebase-client.ts"
      via: "import { auth } for signOut"
      pattern: "signOut.*auth"
    - from: "src/app/page.tsx"
      to: "src/components/Dashboard.tsx"
      via: "imports Dashboard client component"
      pattern: "Dashboard"
---

<objective>
Restructure routing from catch-all to specific pages, create the editor route at /project/[id], enhance Toast for action buttons, and update MusicXML validation for MEI support.

Purpose: Establishes the routing foundation for dashboard at `/` and editor at `/project/[id]`. Toast action support is required for the delete-undo pattern in Plan 03. MEI validation support enables `.mei` files accepted in the updated file validation.

Output: Dashboard page shell, editor page with sign-out and back navigation, enhanced Toast system, MEI-aware validation.
</objective>

<execution_context>
@/Users/emirahmed/.claude/get-shit-done/workflows/execute-plan.md
@/Users/emirahmed/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-project-dashboard-crud/24-RESEARCH.md
@.planning/phases/23-firebase-authentication/23-01-SUMMARY.md
@src/app/[[...slug]]/page.tsx
@src/app/[[...slug]]/client.tsx
@src/hooks/useToast.ts
@src/components/Toast.tsx
@src/lib/musicxmlValidation.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure routes - dashboard at /, editor at /project/[id]</name>
  <files>
    src/app/page.tsx
    src/app/project/[id]/page.tsx
    src/app/project/[id]/client.tsx
  </files>
  <action>
    1. **Delete** the `src/app/[[...slug]]/` directory entirely (both `page.tsx` and `client.tsx`). The catch-all route conflicts with the new specific routes and is no longer needed.

    2. Create `src/app/page.tsx` (dashboard shell):
       - This is a Server Component.
       - Add `export const dynamic = 'force-dynamic'` (prevents static prerender, same as login page pattern).
       - For now, render a minimal placeholder that will be replaced by Plan 03's Dashboard component. Import and render the Dashboard client component (it won't exist yet, so create a minimal placeholder `src/components/Dashboard.tsx` that renders a simple `<div>Dashboard placeholder</div>` -- Plan 03 will replace this). Actually, to avoid file conflicts with Plan 03, just render inline JSX: a `<main>` with a heading "Dashboard" and a "Loading..." message. Plan 03 will replace this entire file with the real Dashboard server component that fetches projects.
       - IMPORTANT: This page MUST exist so the root route works. Even though Plan 03 will rewrite it, it needs to render something valid now so the build succeeds and routing works.

    3. Create `src/app/project/[id]/page.tsx`:
       - Server Component shell.
       - Add `export const dynamic = 'force-dynamic'`.
       - Import `ClientOnly` from `./client`.
       - Accept async params: `{ params }: { params: Promise<{ id: string }> }`.
       - Await params: `const { id } = await params`.
       - Return `<ClientOnly projectId={id} />`.

    4. Create `src/app/project/[id]/client.tsx`:
       - Add `'use client'` directive.
       - Import `dynamic` from `next/dynamic`, `signOut` from `firebase/auth`, `auth` from `@/lib/firebase-client`, `useRouter` from `next/navigation`.
       - Dynamically import App: `const App = dynamic(() => import('../../../App'), { ssr: false })`.
       - Export `function ClientOnly({ projectId }: { projectId: string })`.
       - Add `handleSignOut` async function (same pattern as old catch-all client.tsx): `fetch('/api/auth/session', { method: 'DELETE' })`, then `signOut(auth)`, then `router.push('/login')`.
       - Return JSX with:
         - "Dashboard" back button: `fixed top-3 left-3 z-50`, styled like the sign-out button. On click: `router.push('/')`. Use a left arrow character or text "< Dashboard".
         - "Sign out" button: `fixed top-3 right-3 z-50`, same styling as before.
         - `<App projectId={projectId} />` -- pass projectId prop. NOTE: App.tsx does not yet accept this prop. That's fine -- TypeScript will show a warning but it won't break runtime. Plan 03 or a future phase will wire this up. For now, the prop is passed but App ignores it.
       - Actually, to avoid a TypeScript error: App currently exports `function App()` with no props. To pass `projectId` without breaking the build, either: (a) add a `projectId?: string` prop to App, or (b) pass it as a data attribute. Option (a) is cleaner -- add `interface AppProps { projectId?: string }` to App.tsx and update the function signature to `function App({ projectId }: AppProps)` (unused for now, but typed correctly).
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - `npm run build` succeeds
    - `src/app/[[...slug]]/` directory is deleted
    - `src/app/page.tsx` exists with force-dynamic
    - `src/app/project/[id]/page.tsx` and `client.tsx` exist
    - Editor page passes projectId prop to App
  </verify>
  <done>
    Catch-all route removed. Dashboard placeholder at /. Editor page at /project/[id] with Dashboard back button and Sign out button. App component accepts optional projectId prop.
  </done>
</task>

<task type="auto">
  <name>Task 2: Enhance Toast with action support and update MEI validation</name>
  <files>
    src/hooks/useToast.ts
    src/components/Toast.tsx
    src/lib/musicxmlValidation.ts
  </files>
  <action>
    1. Modify `src/hooks/useToast.ts`:
       - Add optional `action` field to the `Toast` interface: `action?: { label: string; onClick: () => void }`.
       - Add optional `duration` field to the `Toast` interface: `duration?: number`.
       - Update the `show` function signature to accept an optional third parameter for action and duration: `show: (message: string, type: ToastType, options?: { action?: { label: string; onClick: () => void }; duration?: number }) => void`.
       - In the `show` implementation, include the action and duration in the new toast object: `const newToast: Toast = { id, message, type, action: options?.action, duration: options?.duration }`.
       - Change the auto-dismiss timeout from hardcoded `4000` to `options?.duration ?? 4000`.

    2. Modify `src/components/Toast.tsx`:
       - In the `ToastItem` component, update the auto-dismiss useEffect to use `toast.duration ?? 4000` instead of hardcoded 4000 (actually, the auto-dismiss is currently in `useToastProvider` -- check where it lives and update accordingly. The current implementation has the timeout in useToastProvider's `show` function. Keep it there; the duration is already set on the toast object from step 1.)
       - Add an action button rendering in `ToastItem`, between the message `<p>` and the dismiss `<button>`:
         ```
         {toast.action && (
           <button
             onClick={(e) => {
               e.stopPropagation();
               toast.action!.onClick();
               onDismiss(toast.id);
             }}
             className="flex-shrink-0 px-2 py-0.5 text-sm font-semibold underline underline-offset-2 hover:no-underline transition-all"
           >
             {toast.action.label}
           </button>
         )}
         ```
       - When the action button is clicked, it calls the onClick callback AND dismisses the toast.

    3. Modify `src/lib/musicxmlValidation.ts`:
       - Update the `isLikelyMusicXML` function to also accept MEI documents.
       - Add checks for MEI root elements: `const hasMeiRoot = trimmed.includes('<mei')` and `const hasMusicElement = trimmed.includes('<music')`.
       - Update the return statement to: `return (hasXmlDeclaration || true) && (hasScorePartwise || hasScoreTimewise || hasMeiRoot)`. Actually, more precisely: MEI files should also have XML declaration typically, but the key marker is the `<mei` root element. Update to: `return hasScorePartwise || hasScoreTimewise || hasMeiRoot`. Drop the XML declaration requirement since some files may not have it.
       - Rename the function conceptually (but keep the name for backward compat): it now validates both MusicXML and MEI. Add a comment: `// Validates MusicXML and MEI formats (both supported by Verovio)`.
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - `npm run build` succeeds
    - Toast interface has `action` and `duration` optional fields
    - Toast `show` function accepts options parameter
    - ToastItem renders action button when action is present
    - `isLikelyMusicXML` returns true for content containing `<mei` root element
  </verify>
  <done>
    Toast system supports action buttons with callback (needed for delete-undo). Toast duration is configurable (default 4000ms, undo toast will use 5000ms). MEI format recognized by isLikelyMusicXML pre-flight check.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npm run build` succeeds
- Old `[[...slug]]` catch-all directory is deleted
- Root route `/` renders a valid page
- `/project/[id]` loads the App component client-side
- Toast system supports action buttons
- MEI validation works alongside MusicXML validation
</verification>

<success_criteria>
- Dashboard page shell exists at src/app/page.tsx with force-dynamic
- Editor page at /project/[id] loads App with projectId prop and has sign-out + back buttons
- Old catch-all route [[...slug]] is removed
- Toast supports optional action button (label + onClick) and configurable duration
- isLikelyMusicXML recognizes MEI root elements
</success_criteria>

<output>
After completion, create `.planning/phases/24-project-dashboard-crud/24-02-SUMMARY.md`
</output>
