---
phase: 09-osmd-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/lib/getEvents.ts
  - src/lib/animationController.ts
  - src/components/UploadDropZone.tsx
  - src/App.tsx
autonomous: true

must_haves:
  truths:
    - "opensheetmusicdisplay does not appear in package.json"
    - "npm install completes without downloading OSMD"
    - "grep -r opensheetmusicdisplay src/ returns zero results"
    - "npm run build succeeds"
    - "npm run dev serves the application without errors"
  artifacts:
    - path: "package.json"
      provides: "Dependencies without OSMD"
      not_contains: "opensheetmusicdisplay"
    - path: "src/lib/getEvents.ts"
      provides: "Event extraction functions (Verovio only)"
      not_contains: "OpenSheetMusicDisplay"
  key_links:
    - from: "src/lib/getEvents.ts"
      to: "verovio"
      via: "Verovio toolkit API"
      pattern: "VerovioToolkit"
---

<objective>
Remove all traces of OpenSheetMusicDisplay from the codebase.

Purpose: OSMD was the original rendering engine, replaced by Verovio in v1.0. The package and dead code remain in the codebase, adding ~2MB to node_modules and causing confusion. This phase completes the migration cleanup.

Output: A codebase with no OSMD dependency, dead code removed, and clean build.
</objective>

<execution_context>
@/Users/emirahmed/.claude/get-shit-done/workflows/execute-plan.md
@/Users/emirahmed/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/getEvents.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove OSMD package from dependencies</name>
  <files>package.json, package-lock.json</files>
  <action>
1. Edit package.json to remove `"opensheetmusicdisplay": "^1.9.5"` from dependencies
2. Run `npm install` to regenerate package-lock.json without OSMD
3. Verify OSMD is not in node_modules: `ls node_modules | grep -i opensheetmusicdisplay` should return nothing
  </action>
  <verify>
- `grep opensheetmusicdisplay package.json` returns nothing
- `npm ls opensheetmusicdisplay` shows "empty" or not found
- `ls node_modules | grep -i opensheetmusicdisplay` returns nothing
  </verify>
  <done>OSMD package removed from dependencies and node_modules</done>
</task>

<task type="auto">
  <name>Task 2: Remove OSMD imports and dead code from source files</name>
  <files>src/lib/getEvents.ts, src/lib/animationController.ts, src/components/UploadDropZone.tsx, src/App.tsx</files>
  <action>
**src/lib/getEvents.ts:**
- Remove lines 1-5 (OSMD imports: OpenSheetMusicDisplay, VoiceEntry, EngravingRules)
- Remove the entire `getEvents(osmd: OpenSheetMusicDisplay)` function (lines ~20-60) - this is dead code, only `getEventsFromVerovio` is used
- Remove the `getStavenoteIds` helper function (lines ~177-196) - only used by the dead `getEvents` function
- Keep all Verovio-based functions: `getEventsFromVerovio`, `extractTimemapEvents`, `computeEventPositions`
- Keep all type exports: `MusicalEvent`, `MusicalEventWithY`, `TimemapEvent`

**src/lib/animationController.ts:**
- Line 152: Change comment "Call this after OSMD instance is ready" to "Call this after Verovio instance is ready"
- Line 154: Change "@param controllerConfig - Configuration with audio, OSMD, and event getter" to "@param controllerConfig - Configuration with audio, Verovio, and event getter"

**src/components/UploadDropZone.tsx:**
- Line 57: Change comment "// Full OSMD validation" to "// Full MusicXML validation"

**src/App.tsx:**
- Line 55: Change comment "// Debounce scoreScale to avoid OSMD re-render" to "// Debounce scoreScale to avoid Verovio re-render"
- Line 63: Change comment "// Debounce scoreRegion to avoid OSMD re-render" to "// Debounce scoreRegion to avoid Verovio re-render"
- Line 544: Change comment about OSMD layout calculations to Verovio if present (check actual content)
  </action>
  <verify>
- `grep -r "opensheetmusicdisplay" src/` returns nothing
- `grep -r "OSMD" src/` returns nothing (case-sensitive)
- `grep -r "OpenSheetMusicDisplay" src/` returns nothing
- `npm run build` succeeds without errors
- `npm run dev` starts the dev server successfully
  </verify>
  <done>All OSMD references removed from source code, build and dev server work</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -ri "opensheetmusicdisplay\|osmd" src/ package.json` returns nothing
2. `npm run build` completes successfully
3. `npm run dev` starts without errors
4. Application loads in browser and renders a score correctly
</verification>

<success_criteria>
- CLN-01: opensheetmusicdisplay removed from package.json
- CLN-02: All OSMD imports and dead code removed (grep returns zero)
- CLN-03: npm run build and npm run dev both succeed
</success_criteria>

<output>
After completion, create `.planning/phases/09-osmd-cleanup/09-01-SUMMARY.md`
</output>
