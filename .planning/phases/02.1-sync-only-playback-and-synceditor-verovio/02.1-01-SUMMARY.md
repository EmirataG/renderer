# Phase 2.1 Plan 01: Remove BPM Playback, Add Transport Gating Summary

**One-liner:** Removed all BPM-based playback code from RegularRenderer and App.tsx; added sync-only transport gating requiring audio + first/last anchors.

## What Was Done

### Task 1: Remove BPM code from RegularRenderer and add transport gating
- **Commit:** 3bd99b9
- **Files modified:** src/renderers/RegularRenderer.tsx
- Deleted: `bpm` prop, `velocityRef`, `eventEndTimeRef`, `useSyncTiming` variable
- Deleted: `setupEventBPM()` and `animateBPM()` functions (entire BPM animation loop)
- Simplified `play()` to sync-only mode (audio + animateSync)
- Replaced BPM fallback in interpolation effect with empty events array
- Removed `useSyncTiming` from animateSync guard, setTimestamp callback, animation controller
- Added transport gating: `canPlay = hasAudio && hasFirstAnchor && hasLastAnchor`
- Added `transportMessage` for context-aware UI feedback
- Play button disabled until all conditions met
- Net result: -112 lines, +49 lines (63 lines removed)

### Task 2: Remove BPM state and slider from App.tsx
- **Commit:** 834ae12
- **Files modified:** src/App.tsx
- Deleted `bpm` state and `setBpm` setter
- Deleted entire Tempo (BPM) slider UI block
- Removed `bpm={bpm}` prop from RegularRenderer JSX
- Playback section now only contains FPS slider
- Net result: -20 lines

## Verification Results

1. `grep -rn 'bpm|BPM|setupEventBPM|animateBPM|useSyncTiming|velocityRef|eventEndTimeRef' src/renderers/RegularRenderer.tsx src/App.tsx` -- empty (zero hits)
2. `npx tsc --noEmit` -- passes with no type errors
3. `canPlay` and `transportMessage` logic present in RegularRenderer.tsx
4. Sync playback via `animateSync` preserved intact

## Deviations from Plan

None -- plan executed exactly as written.

## Decisions Made

- [02.1-01]: BPM playback is permanently removed; sync-only mode is the sole playback path
- [02.1-01]: Transport gating requires audio + first AND last event sync anchors (not just any two anchors)

## Key Files

- **src/renderers/RegularRenderer.tsx** -- Sync-only renderer with transport gating (canPlay, transportMessage)
- **src/App.tsx** -- Sidebar without BPM slider; Playback section only has FPS

## Duration

~2 min
