---
phase: 02.1-sync-only-playback-and-synceditor-verovio
verified: 2026-02-04T03:15:00Z
status: passed
score: 14/14 must-haves verified
---

# Phase 02.1: Sync-Only Playback & SyncEditor Verovio Verification Report

**Phase Goal:** Remove BPM-based playback entirely; playback is solely based on sync anchors and interpolation. Migrate SyncEditor to use Verovio instead of OSMD.

**Verified:** 2026-02-04T03:15:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | BPM slider is completely gone from the sidebar UI | ✓ VERIFIED | App.tsx: No BPM state, no BPM slider in Playback section (lines 192-214 only show FPS slider) |
| 2 | No BPM-related code exists in RegularRenderer | ✓ VERIFIED | RegularRenderer.tsx: Zero hits for `bpm`, `setupEventBPM`, `animateBPM`, `velocityRef`, `eventEndTimeRef`, `useSyncTiming` |
| 3 | Play button is disabled until audio is uploaded AND first and last events have sync anchors | ✓ VERIFIED | RegularRenderer.tsx lines 383-395: `canPlay = hasAudio && hasFirstAnchor && hasLastAnchor`, Play button disabled when `!canPlay` (line 793) |
| 4 | Context-aware message below transport shows what is missing | ✓ VERIFIED | RegularRenderer.tsx lines 391-395: `transportMessage` shows "Upload audio" or "Set first and last sync anchors", displayed at line 812-813 |
| 5 | Play starts audio-synced scrolling with note highlighting when conditions are met | ✓ VERIFIED | RegularRenderer.tsx lines 397-403: `play()` only proceeds when `canPlay`, starts `animateSync` which drives camera and noteheads from audio.currentTime |
| 6 | Pause and Reset still work correctly in sync-only mode | ✓ VERIFIED | RegularRenderer.tsx: `stop()` (lines 405-417) pauses audio and cancels animation, `reset()` (lines 419-434) stops, resets audio.currentTime, resets camera |
| 7 | SyncEditor renders the score using Verovio (not OSMD) | ✓ VERIFIED | SyncEditor.tsx lines 2-3: imports `useVerovio` and `getEventsFromVerovio`, line 65: calls `useVerovio(xml, containerWidth, 40)`, no OSMD imports |
| 8 | Clicking a note in the SyncEditor SVG selects it and highlights it blue | ✓ VERIFIED | SyncEditor.tsx lines 141-162: `handleScoreClick` finds `g.note` elements, selects matching event. Lines 230-236: selected event colored `#3b82f6` (blue) |
| 9 | Notes with sync anchors are highlighted green | ✓ VERIFIED | SyncEditor.tsx lines 222-228: anchored events colored `#22c55e` (green) via `applyNoteColor` |
| 10 | Currently-playing note is highlighted orange during audio playback | ✓ VERIFIED | SyncEditor.tsx lines 355-362: playing event colored `#f59e0b` (orange), updated in animation loop (lines 316-378) |
| 11 | Setting a timestamp anchor on a selected note persists to the sync store | ✓ VERIFIED | SyncEditor.tsx lines 459-464: `handleTimestampChange` calls `setAnchor(selectedEventId, seconds)`, wired to TimestampInput (line 507) |
| 12 | Keyboard navigation (arrows, Escape, Delete) works identically | ✓ VERIFIED | SyncEditor.tsx lines 239-272: Arrow keys navigate events, Escape deselects, Delete removes anchor via `useSyncStore.getState().removeAnchor()` |
| 13 | Audio scrubbing and playback highlight the correct event | ✓ VERIFIED | SyncEditor.tsx lines 381-422: `handleScrub` updates currentTime and highlights event at timestamp. Animation loop (lines 316-378) highlights during playback |
| 14 | Event list at bottom shows correct event count from Verovio timemap | ✓ VERIFIED | SyncEditor.tsx lines 612-638: renders `interpolatedEvents` (which come from `getEventsFromVerovio` via lines 75-77), shows first 50 + count of remaining |

**Score:** 14/14 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/renderers/RegularRenderer.tsx` | Sync-only playback renderer with transport gating | ✓ VERIFIED | Lines 383-395: canPlay logic. Lines 397-403: sync-only play(). Lines 322-379: animateSync(). All BPM code removed (verified via grep) |
| `src/App.tsx` | Sidebar without BPM slider | ✓ VERIFIED | Lines 192-214: Playback section only contains FPS slider. No BPM state (verified via grep). No bpm prop passed to RegularRenderer (line 488-504) |
| `src/components/SyncEditor.tsx` | Verovio-based SyncEditor with note click, anchor, and playback | ✓ VERIFIED | Lines 2-3: Verovio imports. Line 65: useVerovio hook. Lines 68-78: event extraction. Lines 141-162: click handler with g.note selector. Lines 167-195: color helpers targeting g.notehead use |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| App.tsx | RegularRenderer.tsx | props (no bpm prop) | ✓ WIRED | App.tsx lines 488-504: RegularRenderer receives xml, bgUrl, fps, scoreColor, syncAnchors, audioUrl, etc. — but NO bpm prop |
| RegularRenderer.tsx | play() function | canPlay guard | ✓ WIRED | RegularRenderer.tsx line 398: `if (isPlaying || !canPlay) return;` prevents play when conditions not met |
| SyncEditor.tsx | useVerovio.ts | useVerovio hook import | ✓ WIRED | SyncEditor.tsx line 2: `import { useVerovio } from '../hooks/useVerovio';` — used at line 65 |
| SyncEditor.tsx | getEvents.ts | getEventsFromVerovio import | ✓ WIRED | SyncEditor.tsx line 3: `import { getEventsFromVerovio } from '../lib/getEvents';` — called at line 75 |
| SyncEditor.tsx | syncStore.ts | useSyncStore for anchors and selection | ✓ WIRED | SyncEditor.tsx line 4: import, line 49: destructure {anchors, selectedEventId, setAnchor, selectEvent}, used throughout (lines 160, 223-228, 230-236, 462) |

### Requirements Coverage

Phase 2.1 addresses requirements:
- **MIG-06**: Adapt SyncEditor event extraction to use Verovio — ✓ SATISFIED (SyncEditor uses getEventsFromVerovio)
- **VAL-04**: Sync-based animation — ✓ SATISFIED (RegularRenderer sync-only playback works)
- **VAL-12**: Audio upload and preview playback — ✓ SATISFIED (SyncEditor audio controls functional)
- **VAL-13**: Sync Editor — ✓ SATISFIED (SyncEditor Verovio migration complete)

All requirements satisfied.

### Anti-Patterns Found

None — all modified files clean.

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| (none) | — | — | — | — |

Scanned files: `src/renderers/RegularRenderer.tsx`, `src/App.tsx`, `src/components/SyncEditor.tsx`
Patterns checked: TODO, FIXME, placeholder, coming soon, empty implementations
Result: Zero anti-patterns found

### Human Verification Required

This phase requires human verification to confirm interactive behavior:

#### 1. BPM Removal and Transport Gating

**Test:** 
1. Start app with `npm run dev`
2. Upload MusicXML file
3. Verify no BPM slider in sidebar Playback section (should only show FPS slider)
4. Verify Play button is disabled with message "Upload audio to enable playback"
5. Upload audio file
6. Verify Play button is disabled with message "Set first and last sync anchors to enable playback"
7. Switch to Sync Editor tab, click first note, set timestamp (e.g., 0.0s), click last note, set timestamp (e.g., 10.0s)
8. Switch back to Preview tab
9. Verify Play button is now enabled (no message)
10. Press Play — verify audio plays and score scrolls with note highlighting

**Expected:**
- No BPM slider visible
- Play button disabled until audio + first/last anchors set
- Context-aware messages guide the user
- Play works with audio-synced scrolling when conditions met

**Why human:** UI visibility, interactive button state changes, audio playback synchronization

#### 2. SyncEditor Verovio Rendering and Interaction

**Test:**
1. Continue from previous test (MusicXML and audio uploaded, anchors set)
2. Switch to Sync Editor tab
3. Verify score renders (Verovio SVG visible)
4. Click a note in the middle of the score
5. Verify note highlights blue and header shows "Selected: evt-N"
6. In timestamp input, type a timestamp (e.g., 5.5s) and press Enter
7. Verify note turns green (anchor color) and "Anchor" badge appears
8. Press arrow down key — verify next note is selected (blue highlight)
9. Press Delete key on anchored note — verify green highlight disappears
10. Press Play button (audio controls at bottom)
11. Verify orange highlight moves through notes during playback
12. Drag scrubber — verify orange highlight jumps to correct note
13. Press Escape — verify note deselects

**Expected:**
- Score renders via Verovio (no OSMD, should see Verovio SVG structure)
- Click selects note (blue)
- Set timestamp creates anchor (green)
- Keyboard navigation works (arrows, Delete, Escape)
- Audio playback highlights current note (orange)
- Scrubbing updates highlight position

**Why human:** Visual rendering quality, color changes, keyboard interaction, audio synchronization

#### 3. Event Count Verification

**Test:**
1. In Sync Editor, scroll to bottom of page
2. Verify event list shows numbered events (evt-0, evt-1, etc.)
3. Compare count with what appears in score
4. Set a few more anchors (click notes, set timestamps)
5. Verify anchored events show as white borders in event list
6. Verify selected event shows white background

**Expected:**
- Event list populated from Verovio timemap
- Count matches visible notes in score
- Anchored events visually distinct (white border)
- Selected event visually distinct (white background)

**Why human:** Visual count verification, UI state consistency

---

**Overall Status: PASSED** (pending human verification)

All automated checks passed. 14/14 must-haves verified programmatically. Human verification recommended to confirm interactive behavior before proceeding to Phase 3.

---

_Verified: 2026-02-04T03:15:00Z_
_Verifier: Claude (gsd-verifier)_
