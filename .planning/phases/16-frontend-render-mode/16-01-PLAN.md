---
phase: 16-frontend-render-mode
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderers/RegularRenderer.tsx
  - src/types/global.d.ts
  - src/main.tsx
  - src/RenderApp.tsx
autonomous: true

must_haves:
  truths:
    - "When window.__EXPORT_CONFIG__ is set before page load, RenderApp renders instead of App"
    - "All export settings (scoreColor, fonts, animation params, scoreRegion, syncAnchors) are passed to RegularRenderer from config"
    - "Page virtualization is disabled in render mode -- all SVG pages stay mounted regardless of camera position"
    - "Camera CSS transition is 'none' in render mode -- no 200ms ease-out animation"
    - "window.rendererReady becomes true after animation controller is exposed with interpolated events"
    - "audioDuration is set from config prop in render mode (no audio element needed)"
  artifacts:
    - path: "src/renderers/RegularRenderer.tsx"
      provides: "renderMode and audioDuration props, virtualization bypass, transition disable, rendererReady signal"
      contains: "renderMode"
    - path: "src/RenderApp.tsx"
      provides: "Minimal render-mode wrapper reading __EXPORT_CONFIG__ and rendering RegularRenderer"
      min_lines: 30
    - path: "src/types/global.d.ts"
      provides: "ExportConfig interface and rendererReady/animationController Window declarations"
      contains: "__EXPORT_CONFIG__"
    - path: "src/main.tsx"
      provides: "Dynamic import routing between App and RenderApp based on __EXPORT_CONFIG__"
      contains: "__EXPORT_CONFIG__"
  key_links:
    - from: "src/main.tsx"
      to: "src/RenderApp.tsx"
      via: "dynamic import when __EXPORT_CONFIG__ detected"
      pattern: "import.*RenderApp"
    - from: "src/RenderApp.tsx"
      to: "src/renderers/RegularRenderer.tsx"
      via: "renders RegularRenderer with renderMode={true} and all config props"
      pattern: "renderMode.*true"
    - from: "src/RenderApp.tsx"
      to: "src/stores/syncStore.ts"
      via: "useSyncStore.setState to inject sync anchors before render"
      pattern: "useSyncStore\\.setState"
    - from: "src/renderers/RegularRenderer.tsx"
      to: "window.rendererReady"
      via: "sets rendererReady = true after animation controller exposure"
      pattern: "rendererReady.*=.*true"
---

<objective>
Create the frontend render mode for headless Chrome frame capture. The frontend detects window.__EXPORT_CONFIG__ (injected by Puppeteer's evaluateOnNewDocument), renders a minimal RenderApp component instead of the full App UI, injects all settings into RegularRenderer with virtualization disabled and CSS transitions removed, and signals readiness via window.rendererReady.

Purpose: Phase 17 (Puppeteer Integration) needs a headless-compatible frontend that renders the exact score animation without UI chrome, with all pages mounted and instant camera positioning for frame-accurate screenshots.

Output: Modified RegularRenderer with renderMode prop, new RenderApp.tsx wrapper, updated main.tsx entry routing, ExportConfig type declarations.
</objective>

<execution_context>
@/Users/emirahmed/.claude/get-shit-done/workflows/execute-plan.md
@/Users/emirahmed/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-frontend-render-mode/16-RESEARCH.md
@.planning/phases/15-backend-foundation-settings-transfer/15-01-SUMMARY.md

@src/renderers/RegularRenderer.tsx
@src/main.tsx
@src/types/global.d.ts
@src/stores/syncStore.ts
@export-service/src/shared/exportSettings.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add renderMode and audioDuration props to RegularRenderer with virtualization bypass, transition disable, and readiness signal</name>
  <files>src/renderers/RegularRenderer.tsx</files>
  <action>
Modify RegularRenderer.tsx with four targeted changes:

1. **Props interface** (around line 21): Add two optional props:
   - `renderMode?: boolean` (default false)
   - `audioDuration?: number` (for render mode, overrides audio element duration)
   Add these to the destructured props with defaults: `renderMode = false` and leave audioDuration as optional.

2. **Virtualization bypass** (around line 209-213): Wrap the virtualization activation block in an `if (!renderMode)` guard. After `setEventsInStore(cachedEvents, svgPages)`, the existing block that sets `extractionDoneRef.current = true` and computes `initialVisible` should only run when `!renderMode`. In render mode, `extractionDoneRef.current` stays `false`, so the mounting condition `!extractionDoneRef.current || visiblePages.has(i)` (line 772) keeps ALL pages mounted.

3. **Camera transition** (line 757): Change the inline style from `transition: "transform 200ms ease-out"` to `transition: renderMode ? "none" : "transform 200ms ease-out"`.

4. **audioDuration from prop**: Add a useEffect after the existing audioDuration state declaration (~line 94). When `audioDuration` prop is provided and > 0, call `setAudioDuration(propValue)`. Use a distinct name to avoid shadowing: accept the prop as `audioDuration: propAudioDuration` in destructuring, keep the state variable as `audioDuration`.

5. **rendererReady signal** (in the animation controller useEffect, ~line 701 after the controller object is assigned): Add `(window as any).rendererReady = true;` right after `console.log("[RegularRenderer] Animation controller exposed on window")`. In the cleanup function (line 705-708), add `(window as any).rendererReady = false;` before `destroyAnimationController()`.

6. **getFps update**: Change `getFps: () => 30` to `getFps: () => fps` so it returns the actual fps prop value.

Do NOT modify any other behavior. The renderMode prop only affects virtualization and camera transition. The existing interactive mode (renderMode=false) must work identically.
  </action>
  <verify>
Run `cd /Users/emirahmed/Desktop/Manuscript/renderer && npx tsc --noEmit` to verify TypeScript compiles without errors.
Grep for `renderMode` in RegularRenderer.tsx to confirm all 4 insertion points.
Grep for `rendererReady` in RegularRenderer.tsx to confirm signal is set and cleared.
  </verify>
  <done>
RegularRenderer accepts renderMode and audioDuration props. When renderMode=true: virtualization never activates (all pages mounted), camera transition is "none", audioDuration is set from prop. window.rendererReady is set to true when animation controller is exposed, false on cleanup. TypeScript compiles cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ExportConfig types, entry point routing, and RenderApp component</name>
  <files>src/types/global.d.ts, src/main.tsx, src/RenderApp.tsx</files>
  <action>
Three files to modify/create:

**1. src/types/global.d.ts** -- Replace the entire file with updated Window declarations:
- Keep the existing `export {};` and `declare global` wrapper
- Keep existing window properties (setAnimationFrame, setAnimationTimestamp, getAnimationDuration, isAnimationReady) with their JSDoc comments
- Add new properties:
  - `__EXPORT_CONFIG__?: ExportConfig` -- Config injected by Puppeteer evaluateOnNewDocument
  - `rendererReady?: boolean` -- Readiness signal for backend polling
  - `animationController?: { setFrame: (frameNumber: number, fps?: number) => void; setTimestamp: (seconds: number) => void; getDuration: () => number; getFps: () => number; }` -- Animation control API
- Define the `ExportConfig` interface inside the global scope:
  ```
  interface ExportConfig {
    musicXml: string;
    syncAnchors: Record<string, number>;  // Serialized Map via Object.fromEntries
    audioDuration: number;
    fps: number;
    scoreColor: string;
    scoreShadowDistance: number;
    hideUnplayedNotes: boolean;
    smoothReveal: boolean;
    scoreRegion: { x: number; y: number; width: number; height: number } | null;
    scoreBorder: string;
    scoreScale: number;
    musicFont: string;
    activeNoteheadColor: string | null;
    activeNoteheadScale: number;
    activeNoteheadEntryMs: number;
    activeNoteheadHoldMs: number;
    activeNoteheadExitMs: number;
    colorFullNote: boolean;
    bgUrl: string | null;
  }
  ```
  This mirrors the ExportSettings TypeBox schema from export-service/src/shared/exportSettings.ts plus musicXml, syncAnchors, audioDuration, and bgUrl fields that Phase 17 will inject.

**2. src/main.tsx** -- Replace with dynamic import routing:
```typescript
import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import './index.css'

const exportConfig = window.__EXPORT_CONFIG__;

async function bootstrap() {
  const RootComponent = exportConfig
    ? (await import('./RenderApp')).default
    : (await import('./App')).default;

  createRoot(document.getElementById('root')!).render(
    <StrictMode>
      <RootComponent />
    </StrictMode>,
  );
}

bootstrap();
```
The key insight: `window.__EXPORT_CONFIG__` is set by Puppeteer's `evaluateOnNewDocument` BEFORE any script runs, so this synchronous read is race-free. The dynamic import ensures App code is not loaded in render mode and vice versa.

**3. src/RenderApp.tsx** -- Create new file:
- Import `useState, useEffect` from React, `RegularRenderer` from renderers, `useSyncStore` from stores, `ScoreRegion` from types/score, `BorderStyle` from borders.
- Read config from `window.__EXPORT_CONFIG__!` (non-null assertion OK -- we only render this when config exists).
- Use a `ready` state flag (boolean, initially false).
- In a useEffect (empty deps), inject sync anchors into Zustand via `useSyncStore.setState({ anchors: new Map(Object.entries(config.syncAnchors)) })`, then set `ready = true`.
- If `!ready`, return null (gate rendering until anchors are injected).
- Render a full-viewport div (`width: 100vw, height: 100vh, overflow: hidden`) with the background from `config.bgUrl` (if present, use `url(${config.bgUrl}) center/cover no-repeat`; otherwise black `#000`).
- Inside, render `<RegularRenderer>` with ALL props mapped from config:
  - `xml={config.musicXml}`
  - `bgUrl={config.bgUrl ?? undefined}`
  - `fps={config.fps}`
  - `scoreColor={config.scoreColor}`
  - `syncAnchors={anchors}` -- read from useSyncStore after injection
  - `scoreRegion={config.scoreRegion as ScoreRegion | null}`
  - `scoreBorder={(config.scoreBorder ?? 'none') as BorderStyle}`
  - `scoreScale={config.scoreScale ?? 1}`
  - `musicFont={config.musicFont ?? 'Bravura'}`
  - `activeNoteheadColor={config.activeNoteheadColor ?? undefined}`
  - `activeNoteheadScale={config.activeNoteheadScale ?? 1}`
  - `activeNoteheadAnimationEntryMs={config.activeNoteheadEntryMs ?? 50}`
  - `activeNoteheadAnimationHoldMs={config.activeNoteheadHoldMs ?? 200}`
  - `activeNoteheadAnimationExitMs={config.activeNoteheadExitMs ?? 200}`
  - `colorFullNote={config.colorFullNote ?? false}`
  - `renderMode={true}`
  - `audioDuration={config.audioDuration}`
- Do NOT include audioUrl (no audio element in render mode).
- Do NOT render any UI chrome (no sidebar, tabs, transport bar, upload zone).
  </action>
  <verify>
Run `cd /Users/emirahmed/Desktop/Manuscript/renderer && npx tsc --noEmit` to verify TypeScript compiles without errors.
Run `cd /Users/emirahmed/Desktop/Manuscript/renderer && npx vite build` to verify the production build succeeds (dynamic imports produce correct chunks).
Grep for `__EXPORT_CONFIG__` in main.tsx and global.d.ts to confirm wiring.
Grep for `renderMode.*true` in RenderApp.tsx to confirm prop is passed.
  </verify>
  <done>
ExportConfig interface declared in global.d.ts matching the backend schema + runtime fields. main.tsx dynamically routes to RenderApp when __EXPORT_CONFIG__ exists, App otherwise. RenderApp.tsx reads config, injects sync anchors into Zustand, gates rendering on ready state, renders RegularRenderer with all settings and renderMode=true. Production build succeeds with code splitting (App and RenderApp in separate chunks).
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npx tsc --noEmit` passes -- no TypeScript errors
2. `npx vite build` succeeds -- production build with correct code splitting
3. `grep -n "renderMode" src/renderers/RegularRenderer.tsx` shows props, virtualization guard, and transition conditional
4. `grep -n "rendererReady" src/renderers/RegularRenderer.tsx` shows signal set and cleanup
5. `grep -n "__EXPORT_CONFIG__" src/main.tsx src/types/global.d.ts src/RenderApp.tsx` shows config detection, type declaration, and config reading
6. Normal interactive mode unaffected: App still renders when __EXPORT_CONFIG__ is absent
</verification>

<success_criteria>
- RegularRenderer renderMode prop disables virtualization (extractionDoneRef stays false) and removes camera transition
- RenderApp reads all 19 ExportConfig fields and passes them as RegularRenderer props
- Sync anchors injected into Zustand before RegularRenderer mounts
- window.rendererReady set to true when animation controller is ready
- audioDuration set from config prop (no audio element dependency)
- TypeScript compiles cleanly, Vite builds successfully
- Interactive mode (no __EXPORT_CONFIG__) works identically to before
</success_criteria>

<output>
After completion, create `.planning/phases/16-frontend-render-mode/16-01-SUMMARY.md`
</output>
