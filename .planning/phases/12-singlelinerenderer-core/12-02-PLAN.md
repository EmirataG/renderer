---
phase: 12-singlelinerenderer-core
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Active note stays near center during playback"
    - "Camera movement is smooth (no jumps or jitter)"
    - "Notehead animation visually works (scale and color)"
    - "Transport controls function correctly"
  artifacts: []
  key_links: []
---

<objective>
Verify SingleLineRenderer works correctly with visual inspection of playback.

Purpose: Confirm the horizontal camera tracking, notehead animation, and transport controls work as expected before completing the phase.

Output: User approval that Phase 12 success criteria are met.
</objective>

<execution_context>
@/Users/emirahmed/.claude/get-shit-done/workflows/execute-plan.md
@/Users/emirahmed/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-singlelinerenderer-core/12-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test harness for SingleLineRenderer</name>
  <files>src/app/test-single-line/page.tsx</files>
  <action>
Create a minimal test page that renders SingleLineRenderer with a sample MusicXML file.

```typescript
'use client';

import SingleLineRenderer from '@/renderers/SingleLineRenderer';
import { useState, useEffect } from 'react';

export default function TestSingleLinePage() {
  const [xml, setXml] = useState<string>('');

  // Load a sample MusicXML on mount (or allow file upload)
  useEffect(() => {
    // Try to load a sample file if available, otherwise show upload
    fetch('/sample.xml')
      .then(r => r.ok ? r.text() : '')
      .then(setXml)
      .catch(() => setXml(''));
  }, []);

  const handleFileUpload = (e: React.ChangeEvent<HTMLInputElement>) => {
    const file = e.target.files?.[0];
    if (file) {
      file.text().then(setXml);
    }
  };

  return (
    <div className="p-4">
      <h1 className="text-xl font-bold mb-4">SingleLineRenderer Test</h1>

      <div className="mb-4">
        <input
          type="file"
          accept=".xml,.musicxml,.mxl"
          onChange={handleFileUpload}
          className="border p-2"
        />
      </div>

      {xml ? (
        <div className="border rounded">
          <SingleLineRenderer
            xml={xml}
            scoreColor="#000000"
            scoreScale={1}
            activeNoteheadScale={1.3}
            activeNoteheadColor="#ff0000"
          />
        </div>
      ) : (
        <p>Upload a MusicXML file to test</p>
      )}
    </div>
  );
}
```

Note: This test page is for verification only. The user will need to:
1. Upload a MusicXML file
2. Set sync anchors (first and last events) if transport controls are needed
3. Or simply verify the horizontal layout renders correctly

  </action>
  <verify>
1. `npm run dev` starts without errors
2. Navigate to http://localhost:3000/test-single-line
3. Page loads and accepts file upload
  </verify>
  <done>Test page exists and renders SingleLineRenderer component</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
SingleLineRenderer component with:
- Horizontal section layout using flexbox
- Camera tracking via CSS translateX with smooth transitions
- Notehead animation during playback
- Transport controls (play, stop, reset)
  </what-built>
  <how-to-verify>
1. Start dev server: `npm run dev`
2. Navigate to http://localhost:3000/test-single-line
3. Upload a MusicXML file (e.g., a simple piece with multiple measures)
4. Verify the score renders horizontally (single line, no system breaks)

**Visual checks:**
- [ ] Score displays as one continuous horizontal line
- [ ] Sections are laid out left-to-right with no gaps
- [ ] Score is vertically centered in the viewport

**If audio/anchors available for playback testing:**
- [ ] Click Play - camera should smoothly scroll right
- [ ] Active note stays near center of viewport (not at edges)
- [ ] Noteheads animate (scale up, color change, then return)
- [ ] Camera movement is smooth (no jumps or jitter)
- [ ] Stop pauses playback
- [ ] Reset returns to beginning

**Edge cases:**
- [ ] At score start: left edge is flush (no empty space on left)
- [ ] At score end: right edge is flush (no empty space on right)
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe issues found</resume-signal>
</task>

</tasks>

<verification>
User visually confirms:
1. Horizontal layout renders correctly
2. Camera tracking is smooth
3. Animations work
4. Transport controls function
</verification>

<success_criteria>
- User approves visual verification
- No critical bugs in camera or animation behavior
</success_criteria>

<output>
After completion, create `.planning/phases/12-singlelinerenderer-core/12-02-SUMMARY.md`
</output>
