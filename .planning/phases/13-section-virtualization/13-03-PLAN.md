---
phase: 13-section-virtualization
plan: 03
type: execute
wave: 3
depends_on: ["13-02"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "DOM shows only 3 sections mounted during playback"
    - "Section boundaries invisible to users"
    - "Tied notes/slurs cross boundaries correctly"
    - "No animation glitches during section transitions"
  artifacts: []
  key_links: []
---

<objective>
Visual verification of section virtualization

Purpose: Confirm all Phase 13 success criteria are met through human observation - DOM inspection shows correct virtualization, visual inspection confirms seamless boundaries.

Output: Verified section virtualization with user approval.
</objective>

<execution_context>
@/Users/emirahmed/.claude/get-shit-done/workflows/execute-plan.md
@/Users/emirahmed/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-section-virtualization/13-01-SUMMARY.md
@.planning/phases/13-section-virtualization/13-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Section virtualization for SingleLineRenderer:
- Only 3 sections mounted in DOM during playback (current + 1 buffer on each side)
- Placeholder divs for unmounted sections maintain layout
- Overlap rendering with clip-path for seamless boundaries
- Animation guards prevent querying unmounted sections
  </what-built>
  <how-to-verify>
1. **Start the development server:**
   ```bash
   cd /Users/emirahmed/Desktop/Manuscript/renderer && npm run dev
   ```

2. **Load a long score (>45 measures to get >3 sections):**
   - Open the app in browser
   - Upload a MusicXML file with at least 50 measures (to ensure 4+ sections at 15 measures/section)
   - Switch to SingleLineRenderer mode if not default

3. **DOM Virtualization Test:**
   - Open browser DevTools (F12 or Cmd+Shift+I)
   - Navigate to Elements tab
   - Find the score container (div with class "preview-score")
   - Play the score and observe:
     - Should see only ~3 section divs with SVG content
     - Other section divs should be empty (placeholder divs with width/height but no children)
   - Scroll through the score and verify sections mount/unmount as camera moves

4. **Seamless Boundaries Test:**
   - Watch playback - staff lines should appear as one continuous line
   - No visible gaps, seams, or jumps at section boundaries
   - If score has tied notes at section boundaries, verify ties connect properly
   - If score has slurs crossing sections, verify slurs appear continuous

5. **Animation Test:**
   - Play the score and watch notehead animations
   - Verify animations work smoothly as notes cross section boundaries
   - No missing noteheads or animation glitches when transitioning sections

6. **Short Score Test (optional):**
   - Load a short score (<45 measures, <3 sections)
   - Verify all sections remain mounted (no virtualization for short scores)

7. **Render Mode Test (optional):**
   - Add `?render=true` to URL
   - Verify all sections are mounted (no placeholders) for Puppeteer capture
  </how-to-verify>
  <resume-signal>
Type "approved" if all tests pass, or describe any issues found (gaps, missing notes, animation glitches, etc.)
  </resume-signal>
</task>

</tasks>

<verification>
User confirmation that:
1. DOM shows only 3 sections mounted during playback
2. Section boundaries are invisible (continuous staff lines)
3. Tied notes and slurs render correctly across boundaries
4. No animation glitches during section transitions
</verification>

<success_criteria>
User types "approved" indicating all Phase 13 success criteria are met:
- SEC-03: Lazy loading - only visible sections mounted in DOM
- SEC-04: Section overlap for tied notes/slurs continuity
- HOR-03: Section transitions are visually seamless
</success_criteria>

<output>
After completion, create `.planning/phases/13-section-virtualization/13-03-SUMMARY.md`
</output>
