---
phase: quick
plan: 260327-e04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.tsx
  - src/components/UploadDropZone.tsx
autonomous: true
requirements: [simplify-project-files-section]
must_haves:
  truths:
    - "When a project is loaded (projectId set), the sidebar section title reads 'Background Image' not 'Project Files'"
    - "When a project is loaded, only the Background FileStatusCard is visible (Score and Audio cards are hidden)"
    - "When a project is loaded, drop zone text says 'Drop image here or click to browse'"
    - "When no project is loaded (local mode), all three FileStatusCards remain visible and section title stays 'Project Files'"
  artifacts:
    - path: "src/App.tsx"
      provides: "Conditional section title based on projectId"
    - path: "src/components/UploadDropZone.tsx"
      provides: "Conditional FileStatusCard rendering and updated drop text"
  key_links:
    - from: "src/App.tsx"
      to: "src/components/UploadDropZone.tsx"
      via: "projectId prop"
      pattern: "projectId"
---

<objective>
Simplify the "Project Files" sidebar section when a project is loaded. Rename to "Background Image", hide Score/Audio status cards, and update drop zone text. Local-only mode (no projectId) remains unchanged.

Purpose: When editing a saved project, score and audio are immutable -- showing their status cards is misleading clutter. Focusing the section on background image makes the UI clearer.
Output: Cleaner sidebar section for project-loaded state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/App.tsx (lines 554-565 — upload section with "Project Files" heading)
@src/components/UploadDropZone.tsx (full file — drop zone + FileStatusCards)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Conditional section title and hide Score/Audio cards when project loaded</name>
  <files>src/App.tsx, src/components/UploadDropZone.tsx</files>
  <action>
In src/App.tsx line 555, change the static section title to be conditional on projectId:
- When `projectId` is truthy: render "Background Image"
- When `projectId` is falsy: render "Project Files"

Replace:
```
<h2 className="grunge-section-title">Project Files</h2>
```
With:
```
<h2 className="grunge-section-title">{projectId ? "Background Image" : "Project Files"}</h2>
```

In src/components/UploadDropZone.tsx:

1. Update the drop zone prompt text (line 248). Change:
```
Drop files here or click to browse
```
To be conditional on `projectId`:
- When `projectId` is truthy: "Drop image here or click to browse"
- When `projectId` is falsy: "Drop files here or click to browse"

2. In the "Current Files Status" section (lines 258-293), wrap the Score and Audio FileStatusCard blocks in a conditional that only renders them when `projectId` is falsy:
```tsx
{!projectId && (
  <>
    {/* MusicXML Status */}
    <FileStatusCard
      label="Score"
      ...existing props...
    />

    {/* Audio Status */}
    <FileStatusCard
      label="Audio"
      ...existing props...
    />
  </>
)}
```

The Background Image FileStatusCard (lines 286-292) remains unconditional -- always visible.
  </action>
  <verify>
    <automated>cd /Users/emirahmed/Desktop/Manuscript/renderer && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
- Section title shows "Background Image" when projectId is set, "Project Files" otherwise
- Score and Audio FileStatusCards are hidden when projectId is set
- Drop zone text shows "Drop image here or click to browse" when projectId is set
- TypeScript compiles without errors
- Local-only mode (no projectId) is completely unchanged
  </done>
</task>

</tasks>

<verification>
1. TypeScript compilation passes: `npx tsc --noEmit`
2. Visual check: Open a saved project -- sidebar should show "Background Image" section with only background card and image-focused drop zone
3. Visual check: Create new local project (no projectId) -- sidebar should show "Project Files" with all three cards
</verification>

<success_criteria>
- Section title dynamically switches based on projectId
- Only Background FileStatusCard visible when project is loaded
- Drop zone text updated for image-only context
- No regressions in local-only mode
</success_criteria>

<output>
After completion, create `.planning/quick/260327-e04-simplify-project-files-section-to-backgr/260327-e04-SUMMARY.md`
</output>
